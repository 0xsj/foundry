[package]
name = "url-shortener"
version = "0.1.0"
edition = "2021"

# TODO: This starter has only a binary crate (main.rs).
# Your goal is to add a library crate (lib.rs) and split the
# monolithic main.rs into focused modules.
#
# When you're done, the structure should allow:
#   use url_shortener::Store;
#   use url_shortener::shorten;
# from the binary entry point.

[[bin]]
name = "url-shortener"
path = "main.rs"

[dependencies]
# No external dependencies required
