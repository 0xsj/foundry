[package]
name = "example-crate"
version = "0.1.0"
edition = "2021"
description = "Example crate demonstrating Rust module organization"

[dependencies]
# No external dependencies — stdlib only
# This is where you'd add:
# serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
# Dev-only dependencies available for tests
# assert_matches = "1.5"

[features]
# Optional feature: enable verbose request logging
verbose = []
