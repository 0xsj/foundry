// ============================================================================
// Variables and Types — TypeScript
// ============================================================================
// Fill in each section. Run with: npx ts-node variables.ts
// Or just save and ask for a review.
// ============================================================================

// ============================================================================
// 1. LET vs CONST
// Declare variables using `let` and `const`.
// What's the difference? When would you use each?
// ============================================================================
// a) a let string

// b) a const string

// c) try reassigning the const — what happens?

// ============================================================================
// 2. EXPLICIT TYPES
// Declare variables with explicit type annotations.
// ============================================================================
// a) a string

// b) a number (TypeScript has no int vs float — just `number`)

// c) a boolean

// ============================================================================
// 3. TYPE INFERENCE
// Declare variables WITHOUT type annotations. Let TypeScript infer.
// Hover over them in your editor — what types does TS assign?
// ============================================================================
// a) a string

// b) a number

// c) a boolean

// d) const name = "redis" — what type does TS infer? (hint: it's not `string`)

// ============================================================================
// 4. UNDEFINED and NULL
// TypeScript has both undefined and null. Declare:
// ============================================================================
// a) a variable that is explicitly undefined

// b) a variable that is explicitly null (you'll need a type annotation)

// c) a variable with a union type: string | null

// ============================================================================
// 5. ARRAYS and TUPLES
// Declare:
// ============================================================================
// a) an array of numbers using number[] syntax

// b) the same array using Array<number> syntax

// c) a tuple of [string, number] (e.g. a host and port pair)

// ============================================================================
// 6. PRINT EVERYTHING
// Use console.log to print your variables.
// Use typeof to check runtime types: console.log(typeof myVar)
// ============================================================================

console.log("done");
